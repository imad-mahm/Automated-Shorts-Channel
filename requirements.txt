# Script generation (Gemini) — new unified SDK: `from google import genai`
google-genai>=1.0.0

# Text-to-speech and word-level subtitles
edge-tts>=6.1.0

# HTTP calls (Cloudflare Workers AI image generation)
requests>=2.31.0

# YouTube Data API v3 + OAuth2
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Local .env loading
python-dotenv>=1.0.0

# Fallback captioning / transcription (edge-tts VTT is the primary path)
openai-whisper>=20231117
